getSample is O(n) because the body of the for loop is constant
add is O(1) because there are no loops
getAllAt is O(n) although there are two loops, there are no nested loops and the body of the loops are constant
getLength is O(1) because there are no loops
remove is O(1) because there are no loops
removeAllAt is O(n) the body of the for loop is constant
getInstrument is O(1) there are no loops
